#include<stdio.h>
#include<string.h>

int main()
{
  char a[20] = "Devyansh";
  char b[20] = "Devyansh";

  for(int i = 0; a[i] == b[i]; i++)
    {
      if(a[i] - b[i] == 0])
      {
        printf("String is equal");
      }

      else
      {
        printf("String are not equal");
      }
    }
}
